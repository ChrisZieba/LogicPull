<div class="row">
	<div class="col-lg-12">

		<h1>Change the Status of this Interview</h1>

		<div class="row">
			<div class="col-lg-8">

				<p><strong><%= interview.name %></strong> - <%= interview.description %></p>

				<form role="form" method="post" action="" enctype="multipart/form-data">
					<input type="hidden" name="_csrf" value="<%= token %>"/>
					<input type="hidden" name="interview" value="<%= interview.id %>"/>
					<input type="hidden" name="live" value="<%= interview.live.toString() %>"/>

					<div class="form-group">
						<% if (interview.live) { %>
							<p class="help-block">This interview is curently live to the world. If you change this to false, users will see a splash screen when trying to view the interview.</p>
						<% } else { %>
							<p class="help-block">Making this interview live means making it available for end users to complete. If the interview is still being developed, it is best to keep this private until it is ready.</p>
						<% } %>
					</div>

					<% if (interview.live) { %>
						<button type="submit" class="btn btn-primary">Make this interview private</button>
					<% } else { %>
						<button type="submit" class="btn btn-primary">Make this interview available to all</button>
					<% } %>

					<a class="btn btn-default pull-right" href="/manager/interview/<%= interview.id %>">Cancel</a>
				</form>
			</div>
<!-- 
			<div class="col-lg-4">
				<div class="well">
					<h3>Help</h3>
					<p>You must name the interview something else, and it is recommended that you change the description. This will also make copies of the style sheets used for the deliverables.</p>
				</div>
			</div>	 -->		
		</div>
	</div>
</div>